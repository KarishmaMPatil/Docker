services:
      frontend:
        build: /root/Docker/Frontend/. 
        ports:
          - "80:80" 
        container_name: frontend  
      backend:
        build: /root/Docker/Backend/. 
        ports:
          - "8080:8080" 
        container_name: backend


        